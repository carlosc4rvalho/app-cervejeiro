import React from "react";

export default function Filter({ onSelectFilter }) {
    // Aqui você pode definir a lógica para manipular os filtros selecionados e passá-los para o componente Cards
    
    return (
        <>
            {/* Componente que influenciará o que o componente Cards buscará no banco de dados */}
        </>
    );
}

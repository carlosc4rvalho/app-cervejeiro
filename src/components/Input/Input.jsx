import React from "react";

export default function Inputs({ name, placeholder, options, onChange }) {
    // Aqui você pode usar as props name, placeholder, options e onChange para configurar os selects nas telas de cadastro
    
    return (
        <>
            {/* Componente para criar selects nas telas de cadastro, usando as props name, placeholder, options e onChange */}
        </>
    );
}

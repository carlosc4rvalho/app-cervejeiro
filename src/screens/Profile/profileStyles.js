import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        flex: 1,
        marginTop: 48,
        marginBottom: 16,
        marginHorizontal: 16,
    },
    profile: {
    },
    header: {
        
    },
    content: {
        flexWrap: 'wrap',
    },
    card: {
        padding: 16,
        borderRadius: 10,
        backgroundColor: '#fff',
    },
});

export default styles;